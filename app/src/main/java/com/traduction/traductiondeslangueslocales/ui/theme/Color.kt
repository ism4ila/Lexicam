package com.traduction.traductiondeslangueslocales.ui.theme

import androidx.compose.ui.graphics.Color

// Couleurs principales
val PrimaryLight = Color(0xFF115A9A)      // Bleu primaire
val OnPrimaryLight = Color(0xFFFFFFFF)    // Blanc sur le bleu primaire
val PrimaryContainerLight = Color(0xFFD6E3FF)  // Conteneur bleu clair
val OnPrimaryContainerLight = Color(0xFF001C3B)  // Texte foncé sur conteneur bleu clair

// Couleurs secondaires
val SecondaryLight = Color(0xFF7F5609)    // Brun/Ocre secondaire (rappel des couleurs du Cameroun)
val OnSecondaryLight = Color(0xFFFFFFFF)  // Blanc sur le brun secondaire
val SecondaryContainerLight = Color(0xFFFFDEA4)  // Conteneur brun clair
val OnSecondaryContainerLight = Color(0xFF291800)  // Texte foncé sur conteneur brun clair

// Couleurs tertiaires (pour certains éléments d'accent)
val TertiaryLight = Color(0xFF006D3A)     // Vert (rappel du drapeau camerounais)
val OnTertiaryLight = Color(0xFFFFFFFF)   // Blanc sur le vert
val TertiaryContainerLight = Color(0xFF9CF6B4)  // Conteneur vert clair
val OnTertiaryContainerLight = Color(0xFF00210E)  // Texte foncé sur conteneur vert clair

// Couleurs d'erreur
val ErrorLight = Color(0xFFBA1A1A)        // Rouge erreur
val OnErrorLight = Color(0xFFFFFFFF)      // Blanc sur rouge erreur
val ErrorContainerLight = Color(0xFFFFDAD6)  // Conteneur d'erreur
val OnErrorContainerLight = Color(0xFF410002)  // Texte sur conteneur d'erreur

// Couleurs de fond
val BackgroundLight = Color(0xFFFFFBFF)   // Fond blanc légèrement teinté
val OnBackgroundLight = Color(0xFF1A1C1E)  // Texte sur fond
val SurfaceLight = Color(0xFFFFFBFF)      // Surface principale
val OnSurfaceLight = Color(0xFF1A1C1E)    // Texte sur surface

// Couleurs de mode sombre
val PrimaryDark = Color(0xFFA9C7FF)       // Bleu primaire en sombre
val OnPrimaryDark = Color(0xFF003062)     // Texte bleu foncé sur bleu clair
val PrimaryContainerDark = Color(0xFF00468A)  // Conteneur bleu en sombre
val OnPrimaryContainerDark = Color(0xFFD6E3FF)  // Texte bleu clair sur bleu foncé

val SecondaryDark = Color(0xFFEABD40)     // Brun/Ocre secondaire en sombre
val OnSecondaryDark = Color(0xFF422C00)   // Texte brun foncé sur brun clair
val SecondaryContainerDark = Color(0xFF5F4200)  // Conteneur brun en sombre
val OnSecondaryContainerDark = Color(0xFFFFDEA4)  // Texte brun clair sur brun foncé

val TertiaryDark = Color(0xFF80D99A)      // Vert en sombre
val OnTertiaryDark = Color(0xFF00391C)    // Texte vert foncé sur vert clair
val TertiaryContainerDark = Color(0xFF005229)  // Conteneur vert en sombre
val OnTertiaryContainerDark = Color(0xFF9CF6B4)  // Texte vert clair sur vert foncé

val ErrorDark = Color(0xFFFFB4AB)         // Rouge erreur en sombre
val OnErrorDark = Color(0xFF690005)       // Texte rouge foncé sur rouge clair
val ErrorContainerDark = Color(0xFF93000A)  // Conteneur d'erreur en sombre
val OnErrorContainerDark = Color(0xFFFFDAD6)  // Texte rouge clair sur rouge foncé

val BackgroundDark = Color(0xFF1A1C1E)    // Fond sombre
val OnBackgroundDark = Color(0xFFE2E2E6)  // Texte sur fond sombre
val SurfaceDark = Color(0xFF1A1C1E)       // Surface principale en sombre
val OnSurfaceDark = Color(0xFFE2E2E6)     // Texte sur surface en sombre